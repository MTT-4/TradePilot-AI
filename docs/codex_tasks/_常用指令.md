# Codex 常用指令（复制即用）

> AGENTS.md 已写入约束并会被 codex 自动读取，所以下面指令可以很短。
> 用法：在仓库目录执行 `codex "<把对应段落粘进来>"`。建议先发「①核验」。

---

## ① 核验我已写好的 G1–G5 代码（最该先做）

```
review 新增的 apps/web/app/api/skills 和对应 server 目录（quotation/follow-up/currency/
templates/orders/compliance/analytics/usage/audit/exports，以及 leads/inquiry-detection.ts、
kb/knowledge-reference.ts、crm/crm-auto-entry.ts）。检查：租户隔离是否到位、是否漏走
model-gateway、隐私数据是否强制本地、与 AGENTS.md 是否冲突。然后运行
npm run lint && npm run typecheck，把错误和改进建议列给我，先不要改代码。
```

一行版：
```bash
codex "review apps/web/app/api/skills 及对应 server 新增文件，跑 lint+typecheck，列问题，先别改代码"
```

---

## ② 给 skill 加前端入口（要在界面上用时）

```
为已有接口 /api/skills/quotation、/api/skills/follow-up、/api/skills/compliance-risk 各加一个
前端入口页，复用 app/globals.css 组件类与 app-shell，不套整页背景、不动现有页面的数据获取与权限逻辑。
完成后跑 npm run lint && npm run typecheck，列出新增文件。
```

---

## ③ "需批准"的现有文件增强（会改现有文件，单独点名）

first_response_draft 增强：
```
本次允许修改 server/replies/service.ts：为首封回复增加 questions_to_confirm /
uncertain_points / next_action，存入 Reply.citations.meta，不改 schema、不改 draftText 纯文本。
完成后跑 npm run lint && npm run typecheck。
```

customer_scoring 增强：
```
本次允许修改 server/leads/scoring.ts：把 reasons 升级为带 factor/impact 的结构，
最后仍 join 成 Lead.scoreReason 单字符串存库，不改 schema。完成后跑 lint+typecheck。
```

---

## 建议顺序

先 ① 核验干净 → 再按需要做 ② 或 ③。一次只发一条，看完 codex 的 diff 再继续。
