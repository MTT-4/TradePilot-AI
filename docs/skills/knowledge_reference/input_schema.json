{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "knowledge_reference.input",
  "type": "object",
  "required": ["query"],
  "properties": {
    "query": { "type": "string", "minLength": 1 },
    "top_k": { "type": "integer", "minimum": 1, "maximum": 20 },
    "required_fields": {
      "type": "array",
      "items": { "type": "string" },
      "description": "本次询盘需要覆盖的字段，如 moq/packaging/certification/lead_time"
    }
  },
  "additionalProperties": false
}
